## preamble
MACHINEOVERRIDES =. "${@['', 'xzu90-module:']['xzu90-module' != '${MACHINE}']}"

## based on
require conf/machine/zynqmp-generic.conf

## regular settings
PMU_FIRMWARE_IMAGE_NAME = "pmu-firmware-zynqmp"

## postabmle
PACKAGE_EXTRA_ARCHS:append = "${@['', ' xzu90_module']['xzu90-module' != "${MACHINE}"]}"
