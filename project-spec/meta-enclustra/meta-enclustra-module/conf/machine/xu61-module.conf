## preamble
MACHINEOVERRIDES =. "${@['', 'xu61-module:']['xu61-module' != '${MACHINE}']}"

## based on
require conf/machine/zynqmp-generic.conf

## regular settings
PMU_FIRMWARE_IMAGE_NAME = "pmu-firmware-zynqmp"

## postabmle
PACKAGE_EXTRA_ARCHS:append = "${@['', ' xu61_module']['xu61-module' != "${MACHINE}"]}"
