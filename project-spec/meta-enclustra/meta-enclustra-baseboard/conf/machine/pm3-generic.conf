## machine.conf for the pm3 carrier board

## preamble
MACHINEOVERRIDES =. "${@['', 'pm3-generic:']['pm3-generic' != '${MACHINE}']}"

## module based on:
#require conf/machine/zynq-generic.conf

## postabmle
PACKAGE_EXTRA_ARCHS:append = "${@['', ' pm3-generic']['pm3-generic' != "${MACHINE}"]}"
