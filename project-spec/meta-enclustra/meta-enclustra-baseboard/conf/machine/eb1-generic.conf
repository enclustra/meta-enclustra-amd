## machine.conf for the eb1 carrier board

## preamble
MACHINEOVERRIDES =. "${@['', 'eb1-generic:']['eb1-generic' != '${MACHINE}']}"

## module based on:
#require conf/machine/zynq-generic.conf

## postabmle
PACKAGE_EXTRA_ARCHS:append = "${@['', ' eb1-generic']['eb1-generic' != "${MACHINE}"]}"
