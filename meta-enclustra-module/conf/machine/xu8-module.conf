## preamble
MACHINEOVERRIDES =. "${@['', 'xu8-module:']['xu8-module' != '${MACHINE}']}"

require conf/machine/zynqmp-override.inc

## postabmle
PACKAGE_EXTRA_ARCHS:append = "${@['', ' xu8_module']['xu8-module' != "${MACHINE}"]}"
